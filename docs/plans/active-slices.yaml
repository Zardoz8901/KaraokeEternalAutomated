- id: redux-modernization
  status: blocked
  owner: unassigned
  branch: refactor/redux-modernization
  base: main
  worktree:
    path: /home/steph/projects/KaraokeEternalAutomated-redux-modernization
    state: missing
  merge_order: 10
  goal: Modernize Orchestrator API/editor/reference flows while preserving Hydra visualizer behavior.
  write_scope:
    - src/routes/Orchestrator/components/
    - src/routes/Orchestrator/views/
    - src/routes/Player/components/Player/PlayerVisualizer/
  protected_paths:
    - server/
    - src/routes/Queue/
    - src/routes/Account/
    - docs/plans/
    - .github/workflows/
  verification:
    - npm test -- src/routes/Orchestrator/components/ApiReference.test.tsx src/routes/Orchestrator/components/CodeEditor.test.tsx src/routes/Player/components/Player/PlayerVisualizer/hydraEvalCode.test.ts
    - npm run typecheck
    - npm run lint
  acceptance:
    - Existing Orchestrator code editor and Hydra visualizer behavior remains covered by focused tests.
    - Branch has a dedicated worktree before review or implementation resumes.
    - Branch is rebased or reconciled with main before merge because it is behind main.
  non_goals:
    - Do not change queue, room, auth, guest, or video proxy behavior.
    - Do not alter server socket action contracts.
    - Do not modify repo work-control files from this feature branch.
  notes:
    - Branch exists locally and on origin but no dedicated worktree is currently checked out.
    - Branch is not contained in main.
    - Touches high-risk Player visualizer files; follow AGENTS.md high-risk protocol before code changes.
  next:
    - implementation
    - review

- id: redux-modernization-decommission
  status: planned
  owner: codex
  branch: docs/redux-modernization-decommission
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 175
  goal: Formally retire the blocked refactor/redux-modernization branch (its headline Orchestrator API-lab feature already shipped on main as 087e3e83; it is 2 ahead and 182 behind main and not an ancestor) so the Gate 3 implementation slices can claim the Orchestrator write scope without a phantom serialization conflict.
  write_scope:
    - docs/plans/active-slices.yaml
    - docs/analysis/redux_modernization_decommission_2026_05_30.md
    - docs/analysis/current_state_2026_03_05.md
  protected_paths:
    - src/
    - server/
    - shared/
    - package.json
    - package-lock.json
    - flake.nix
    - .github/workflows/
    - docs/architecture/
  verification:
    - npm run slices:check
    - npm run typecheck
  acceptance:
    - 'The redux-modernization record status is moved from blocked to a terminal decommissioned state with a decision note (or removed) in active-slices.yaml.'
    - 'An ADR (redux_modernization_decommission_2026_05_30.md, Context/Decision/Consequences) records that the headline feature already shipped on main as 087e3e83, the branch is 2 ahead and 182 behind and not an ancestor, the conflict surface, and the decision to retire rather than reconcile.'
    - 'Any genuinely-unique salvageable delta is identified for separate re-implementation off main (do not cherry-pick the stale branch); if none, that is stated explicitly.'
    - The local and origin refactor/redux-modernization branches are deleted only after the decision is recorded.
    - The serialize-ahead-of-redux-modernization constraint on prior Orchestrator slices is no longer live once this lands.
  non_goals:
    - Do not reconcile or merge the stale branch into main.
    - 'Do not cherry-pick the 182 stale commits onto a long-lived branch; any salvage is a fresh minimal TDD change off main.'
    - Do not change product code in this slice; salvage is a separate slice.
  notes:
    - 'User decision locked 2026-05-30: register redux decommission as a gating slice before Gate 3 implementation.'
    - 'Gating prerequisite for Gate 3b..3f: redux-modernization write_scope (src/routes/Orchestrator/components/ plus views/ plus Player/PlayerVisualizer/) is the exact surface those impl slices edit, and slices:check does not enforce write_scope disjointness, so the collision is process-enforced only.'
    - 'Likely salvage candidate to evaluate: f0614609 send-ack-for-unchanged-code - verify whether main Phase 7A ack/run-id logic already covers it before salvaging.'
    - 'Control-plane writes serialize with the other planned slices on active-slices.yaml and current_state; rebase as needed.'
  next:
    - implementation

