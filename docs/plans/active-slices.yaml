- id: redux-modernization
  status: blocked
  owner: unassigned
  branch: refactor/redux-modernization
  base: main
  worktree:
    path: /home/steph/projects/KaraokeEternalAutomated-redux-modernization
    state: missing
  merge_order: 10
  goal: Modernize Orchestrator API/editor/reference flows while preserving Hydra visualizer behavior.
  write_scope:
    - src/routes/Orchestrator/components/
    - src/routes/Orchestrator/views/
    - src/routes/Player/components/Player/PlayerVisualizer/
  protected_paths:
    - server/
    - src/routes/Queue/
    - src/routes/Account/
    - docs/plans/
    - .github/workflows/
  verification:
    - npm test -- src/routes/Orchestrator/components/ApiReference.test.tsx src/routes/Orchestrator/components/CodeEditor.test.tsx src/routes/Player/components/Player/PlayerVisualizer/hydraEvalCode.test.ts
    - npm run typecheck
    - npm run lint
  acceptance:
    - Existing Orchestrator code editor and Hydra visualizer behavior remains covered by focused tests.
    - Branch has a dedicated worktree before review or implementation resumes.
    - Branch is rebased or reconciled with main before merge because it is behind main.
  non_goals:
    - Do not change queue, room, auth, guest, or video proxy behavior.
    - Do not alter server socket action contracts.
    - Do not modify repo work-control files from this feature branch.
  notes:
    - Branch exists locally and on origin but no dedicated worktree is currently checked out.
    - Branch is not contained in main.
    - Touches high-risk Player visualizer files; follow AGENTS.md high-risk protocol before code changes.
  next:
    - implementation
    - review

- id: hydra-control-boundary
  status: merged_to_main
  owner: codex
  branch: phase-2-hydra-control-boundary
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 20
  goal: Enforce the Hydra room control boundary so collaborators can only broadcast server-validated saved presets while owners/admins retain live-code control.
  write_scope:
    - docs/plans/active-slices.yaml
    - docs/analysis/
    - server/Player/
    - server/socket.ts
    - server/serverWorker.ts
    - server/Rooms/router.ts
  protected_paths:
    - src/routes/Orchestrator/
    - src/routes/Player/components/Player/PlayerVisualizer/
    - server/VideoProxy/
    - server/lib/Database.ts
    - server/lib/bcrypt.ts
    - flake.nix
  verification:
    - npm test -- server/Player/socket.test.ts
    - npm run slices:check
    - npm run typecheck
    - npm run lint
    - npm test
  acceptance:
    - Owner/admin sockets can still broadcast arbitrary Hydra code.
    - Collaborator and guest sockets cannot broadcast arbitrary Hydra code.
    - Collaborator and guest sockets can broadcast only server-loaded DB presets allowed by room policy.
    - Visualizer replay state is attributed and cleared on room lifecycle cleanup.
  non_goals:
    - Do not change Orchestrator UI behavior in this slice.
    - Do not alter Hydra browser execution isolation.
    - Do not change video proxy, auth crypto, database wrapper, queue, or Nix behavior.
  notes:
    - "Authority decision locked 2026-05-19: preset-limited collaborators, arbitrary code only for owner/admin."
    - Gallery presets are client-bundled and remain owner/admin-only until a server-validatable gallery source exists.
    - High-risk visualizer runtime policy change; follow AGENTS.md high-risk and red-team protocols.
    - Merged to main on 2026-05-19.
  next:
    - cleanup

- id: orchestrator-synthesis-style-guide
  status: merged_to_main
  owner: codex
  branch: docs/orchestrator-synthesis-style-guide
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 30
  goal: Document Orchestrator-first synthesis UI style guidance grounded in NN/g heuristics, progressive disclosure, complex-app UX, and accessible synthesis controls.
  write_scope:
    - docs/plans/active-slices.yaml
    - docs/ARCHITECTURE.md
    - docs/architecture/
    - docs/analysis/current_state_2026_03_05.md
  protected_paths:
    - src/
    - server/
    - shared/
    - package.json
    - package-lock.json
    - flake.nix
    - .github/workflows/
  verification:
    - rg -n "orchestrator-synthesis-ui-style-guide|Orchestrator Synthesis UI Style Guide" docs
    - npm run slices:check
    - npm run typecheck
    - npm run lint
  acceptance:
    - Style guide is discoverable from architecture docs.
    - Guide defines Orchestrator-first progressive synthesis UI principles.
    - Guide records owner/admin live-code vs collaborator preset-only UX authority.
    - Slice record tracks completion without force-adding ignored analysis tracker files.
  non_goals:
    - Do not change runtime UI behavior in this slice.
    - Do not alter server socket contracts or room-policy enforcement.
    - Do not redesign the whole app-wide visual system yet.
  notes:
    - "User decision locked 2026-05-19: Orchestrator-first repo style guide using progressive synth UI."
    - Docs-only slice; no production-code TDD target.
    - Merged to main on 2026-05-19.
  next:
    - cleanup

- id: orchestrator-capability-ux
  status: merged_to_main
  owner: codex
  branch: phase-2-orchestrator-capability-ux
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 40
  goal: Align Orchestrator UI dispatch paths with the backend Hydra authority boundary so collaborators use preset-operator workflows and cannot trigger raw-code sends from UI paths.
  write_scope:
    - docs/analysis/
    - docs/plans/active-slices.yaml
    - src/routes/Orchestrator/
  protected_paths:
    - server/
    - shared/actionTypes.ts
    - shared/types.ts
    - src/routes/Player/
    - src/components/App/
    - package.json
    - package-lock.json
    - flake.nix
    - .github/workflows/
  verification:
    - npm test -- src/routes/Orchestrator/components/orchestratorCapabilities.test.ts src/routes/Orchestrator/components/PresetTree.test.tsx src/routes/Orchestrator/components/PresetPicker.test.tsx src/routes/Orchestrator/views/orchestratorViewHelpers.test.ts
    - npm run slices:check
    - npm run typecheck
    - npm run lint
  acceptance:
    - Raw code send dispatch is gated in Orchestrator handlers, not only hidden in UI.
    - Collaborators and guests can send only saved DB presets when both room policies allow it.
    - Gallery/raw-code send paths are manager-only.
    - PresetTree click and keyboard send paths share the same send predicate.
    - Existing preset/folder CRUD permissions remain admin-or-author.
  non_goals:
    - Do not change backend socket contracts or enforcement.
    - Do not change room prefs APIs or admin room-policy management.
    - Do not perform the Solarized restyle in this slice.
    - Do not resume or merge redux-modernization.
  notes:
    - "Red-team fix locked 2026-05-19: gate dispatch functions and keyboard/click send paths, not only rendering."
    - Overlaps blocked redux-modernization Orchestrator paths; serialize this slice and do not run both branches in parallel.
    - "Implemented 2026-05-20: capability helper gates Orchestrator raw-code, gallery, and preset send UX without backend contract changes."
    - Merged to main on 2026-05-20.
  next:
    - cleanup

- id: orchestrator-solarized-foundation-ux
  status: merged_to_main
  owner: codex
  branch: design/orchestrator-solarized-foundation-ux
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 50
  goal: Apply a Solarized, token-driven Orchestrator foundation with Figma-level shell polish, status clarity, and deterministic visual QA.
  write_scope:
    - docs/analysis/
    - docs/architecture/orchestrator-synthesis-ui-style-guide.md
    - docs/plans/active-slices.yaml
    - src/routes/Orchestrator/
  protected_paths:
    - server/
    - shared/actionTypes.ts
    - shared/types.ts
    - src/routes/Player/
    - src/routes/Queue/
    - src/routes/Account/
    - package.json
    - package-lock.json
    - flake.nix
    - .github/workflows/
  verification:
    - npm test -- src/routes/Orchestrator/views/orchestratorLayout.test.ts src/routes/Orchestrator/views/orchestratorViewHelpers.test.ts src/routes/Orchestrator/components/orchestratorCapabilities.test.ts
    - npm test -- src/routes/Orchestrator/components/orchestratorStatus.test.ts src/routes/Orchestrator/components/OrchestratorStatusStrip.test.tsx src/routes/Orchestrator/components/orchestratorColorAudit.test.ts src/routes/Orchestrator/components/PresetBrowser.test.ts
    - npm run slices:check
    - npm run typecheck
    - npm run lint
    - npm run build
  acceptance:
    - Orchestrator shell, Stage header, CodeEditor, CodeMirror theme, Hydra syntax highlighting, presets, API reference, and scoped Orchestrator modals use Solarized tokens instead of one-off GitHub/neon colors.
    - Stage header status strip shows Host live coding, Preset operator, or Policy blocked, plus broadcast and camera state without colliding with preset, camera, or buffer controls.
    - Every PresetBrowser Modal receives the scoped Orchestrator modal class.
    - Deterministic color audit passes for touched Orchestrator files.
    - Desktop and mobile visual QA is completed through authenticated Playwright when feasible, otherwise documented as manual QA with exact viewport evidence.
  non_goals:
    - Do not change backend socket contracts or enforcement.
    - Do not change room prefs APIs or admin room-policy management.
    - Do not redesign app-wide queue, account, player, or shared Modal defaults.
    - Do not add new runtime dependencies.
    - Do not resume or merge redux-modernization.
  notes:
    - "User decision locked 2026-05-20: Orchestrator-first foundation + shell polish, code plus docs, include CodeMirror, Stage-header status strip, scoped Orchestrator modals."
    - CodeMirror tokenization includes both CodeEditor.tsx theme colors and hydraHighlightStyle.ts syntax colors.
    - "Implementation note 2026-05-20: authenticated Playwright screenshots remain manual QA until Nix browser wiring is fixed; the reusable setup/login auth path exists in e2e/smoke.spec.ts."
    - "Validation note 2026-05-20: local Chromium e2e is blocked on Nix because the dev shell does not provide a Nix-built browser or PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH; downloaded Playwright Chromium fails with stub-ld."
    - Merged to main on 2026-05-20 as 8657488f.
    - Stale merged worktrees and branches exist and are cleanup candidates, but cleanup is outside this slice.
  next:
    - cleanup

- id: orchestrator-menu-productization
  status: merged_to_main
  owner: codex
  branch: phase-3-orchestrator-menu-productization
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 60
  goal: Make Visuals a discoverable first-class app area with role-aware Orchestrator host/operator workspaces and Nix-runnable Playwright visual QA.
  write_scope:
    - docs/plans/active-slices.yaml
    - docs/analysis/
    - docs/architecture/orchestrator-synthesis-ui-style-guide.md
    - flake.nix
    - src/components/Navigation/
    - src/components/App/Routes/
    - src/store/middlewareOptions.ts
    - src/store/store.ts
    - src/store/store.test.ts
    - src/routes/Orchestrator/
    - e2e/
  protected_paths:
    - server/
    - shared/actionTypes.ts
    - shared/types.ts
    - src/routes/Player/
    - src/routes/Queue/
    - src/routes/Account/
    - package.json
    - package-lock.json
    - .github/workflows/
  verification:
    - npm test -- src/components/Navigation src/components/App/Routes src/routes/Orchestrator
    - npm run slices:check
    - npm run typecheck
    - npm run lint
    - npm run build
    - nix develop -c npm run test:e2e -- --project=chromium
  acceptance:
    - Visuals appears in bottom app navigation with visible icon-and-label treatment when /orchestrator route access allows it.
    - Bottom app navigation labels remain visible and unclipped with four items on mobile.
    - Orchestrator host layout preserves Presets/API, Stage, and Code; operator/browse layout expands Stage and hides Code/API.
    - Orchestrator status distinguishes Host live coding, Preset operator, Browse only, and Policy blocked.
    - Preset panel empty and policy states explain saved preset availability and send authority.
    - Nix dev shell runs Chromium Playwright without the downloaded-browser stub-ld failure.
  non_goals:
    - Do not change backend socket contracts or enforcement.
    - Do not change room prefs APIs or admin room-policy management.
    - Do not modify package scripts, package lockfiles, or GitHub workflows.
    - Do not alter Player, Queue, Account, server, or shared action/type contracts.
    - Do not resume or merge redux-modernization.
  notes:
    - "User decision locked 2026-05-20: Visuals is a first-class bottom-nav destination serving host plus operator audiences."
    - This slice overlaps blocked redux-modernization Orchestrator component/view paths; serialize this slice ahead of redux-modernization and do not run both branches in parallel.
    - Library escape is navigation, not an Orchestrator workspace tab.
    - Merged to main on 2026-05-20 as 5cdbafae.
  next:
    - cleanup

- id: orchestrator-workspace-model
  status: merged_to_main
  owner: codex
  branch: phase-4-orchestrator-workspace-model
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 70
  goal: Extract Orchestrator runtime decisions into tested pure workspace models plus a thin integration hook while preserving visualizer, camera, and preset behavior.
  write_scope:
    - docs/plans/active-slices.yaml
    - docs/analysis/
    - src/routes/Orchestrator/views/
    - src/routes/Orchestrator/components/
    - e2e/
  protected_paths:
    - server/
    - shared/actionTypes.ts
    - shared/types.ts
    - src/routes/Player/
    - src/routes/Queue/
    - src/routes/Account/
    - package.json
    - package-lock.json
    - flake.nix
    - .github/workflows/
  verification:
    - npm test -- src/routes/Orchestrator
    - npm test
    - npm run typecheck
    - npm run lint
    - npm run slices:check
    - npm run build
    - nix develop -c npm run test:e2e -- --project=chromium
  acceptance:
    - OrchestratorView delegates runtime decisions to a workspace hook/model while still composing StagePanel, CodeEditor, PresetBrowser, ApiReference, and OrchestratorStatusStrip directly.
    - useOrchestratorWorkspace remains a thin integration facade over Redux, reducer injection, current-room fetch, camera sender wiring, timers, and pure helper state machines.
    - Focused tests preserve send ack timing, remote replacement/counting, pre-edit sync, preset-index auto-apply, resize bounds, and panel normalization behavior.
    - Camera relay lifecycle remains unchanged and manual camera/orchestrator validation is logged in the analysis note.
    - No backend, socket, shared contract, package, Nix, or workflow changes are introduced.
  non_goals:
    - Do not change backend socket contracts or enforcement.
    - Do not change room prefs APIs, route guards, or admin room-policy management.
    - Do not introduce a player-applied visualizer ack protocol.
    - Do not add new user-visible preset indicators in this slice.
    - Do not resume or merge redux-modernization.
  notes:
    - "User decision locked 2026-05-21: next architecture slice is Orchestrator workspace-model extraction after Visuals productization."
    - This slice overlaps blocked redux-modernization Orchestrator component/view paths; serialize this slice ahead of redux-modernization and do not run both branches in parallel.
    - High-risk because useCameraSender ownership moves behind the workspace facade; keep the camera hook unchanged and record manual validation outcomes.
    - The hook must return typed plain prop objects and state, not JSX.
    - Merged to main on 2026-05-21 as d322f8cb.
  next:
    - cleanup

- id: orchestrator-preset-operator-ux-spec
  status: merged_to_main
  owner: codex
  branch: phase-5-orchestrator-preset-operator-ux-spec
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 80
  goal: Define the Preset operator and Browse-only Orchestrator UX contract before runtime UI implementation.
  write_scope:
    - docs/plans/active-slices.yaml
    - docs/ARCHITECTURE.md
    - docs/architecture/
    - docs/analysis/
  protected_paths:
    - src/
    - server/
    - shared/
    - package.json
    - package-lock.json
    - flake.nix
    - e2e/
    - .github/workflows/
  verification:
    - rg -n "Preset Operator UX|orchestrator-preset-operator-ux" docs
    - npm run slices:check
    - npm run typecheck
    - npm run lint
  acceptance:
    - Preset Operator UX spec defines modes, action matrix, state truth model, state copy matrix, placement rules, capability classification, protocol gaps, acceptance criteria, and out-of-scope.
    - Load is explicitly defined as local Stage preview and selected preset only, with no room broadcast and no player-applied claim.
    - Toolbar actions and row actions are specified for Host, Operator, Browse-only, and Guest across Gallery presets, Saved DB presets, Folders, and Toolbar.
    - Style guide links to the spec and separates Orchestrator user modes from the Player runtime role.
    - No runtime code, backend, socket, package, Nix, e2e, or workflow changes are introduced.
  non_goals:
    - Do not implement runtime UI changes in this docs-first slice.
    - Do not change backend socket contracts or enforcement.
    - Do not change room prefs APIs, route guards, or admin room-policy management.
    - Do not introduce a player-applied visualizer ack protocol.
    - Do not resume or merge redux-modernization.
  notes:
    - "User decision locked 2026-05-21: Preset operator is the next UX priority, docs first."
    - The next runtime slice should consume this spec and add focused tests for component/helper changes.
    - Existing stale merged worktrees and branches are cleanup candidates but outside this slice.
    - Merged to main on 2026-05-21 as dfd692ef.
  next:
    - cleanup

- id: orchestrator-preset-operator-runtime-ux
  status: merged_to_main
  owner: codex
  branch: phase-6-orchestrator-preset-operator-runtime-ux
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 90
  goal: Implement the client-only Preset operator and Browse-only runtime UX contract without backend, socket, route guard, package, Nix, workflow, or Player changes.
  write_scope:
    - docs/plans/active-slices.yaml
    - docs/architecture/orchestrator-preset-operator-ux.md
    - docs/analysis/
    - src/routes/Orchestrator/components/
  protected_paths:
    - server/
    - shared/actionTypes.ts
    - shared/types.ts
    - src/routes/Player/
    - src/routes/Queue/
    - src/routes/Account/
    - src/routes/Orchestrator/views/
    - package.json
    - package-lock.json
    - flake.nix
    - e2e/
    - .github/workflows/
  verification:
    - npm test -- src/routes/Orchestrator/components/presetEmptyState.test.ts src/routes/Orchestrator/components/PresetTree.test.tsx src/routes/Orchestrator/components/PresetPicker.test.tsx src/routes/Orchestrator/components/PresetBrowser.test.tsx
    - npm test -- src/routes/Orchestrator/components/orchestratorCapabilities.test.ts src/routes/Orchestrator/views/orchestratorShellModel.test.ts
    - npm run slices:check
    - npm run typecheck
    - npm run lint
    - npm test
  acceptance:
    - Operator and Browse-only Presets surfaces hide host-management clutter, including New Folder, Save Preset, gallery Save copy, rename, move, delete, set start, and set player folder.
    - Stable preset keys use preset:<number> and gallery:<string>; selected and loaded preview keys clear when missing from the scoped tree.
    - Load locally previews and selects only; Send does not claim local preview or player-applied state.
    - Send is enabled only for allowed saved DB presets; disabled Send explains room policy or party-folder restriction without broad policy copy repeated on every row.
    - Non-host gallery presets are clearly preview-only.
    - Guest users do not see management actions backend would reject.
    - Named Load/Send actions, touch-target sizing, reduced-motion handling, and Solarized token usage meet the visual design contract.
    - Permanent UX docs record the Phase 6 non-host management decision.
  non_goals:
    - Do not change backend, socket, room prefs, route guards, database, package, Nix, workflow, e2e, or Player visualizer behavior.
    - Do not add player-applied or live-preset labels.
    - Do not add a new socket/player acknowledgement protocol.
    - Do not add deeper preset analytics or experimental skill-clock/sphere UI.
    - Do not resume or merge redux-modernization.
  notes:
    - "User decision locked 2026-05-21: Phase 6 hides all non-host management actions so Operator/Browse-only are audition/send surfaces, not reduced host tools."
    - This slice overlaps blocked redux-modernization Orchestrator component paths; serialize this slice ahead of redux-modernization and do not run both branches in parallel.
    - Hidden UI is not authorization; server-side enforcement remains unchanged.
    - Merged to main on 2026-05-21 as b711ae50.
  next:
    - cleanup

- id: visualizer-applied-runtime-truth
  status: merged_to_main
  owner: codex
  branch: phase-7a-visualizer-applied-runtime-truth
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 100
  goal: Make the Player authoritative for visualizer applied state using server-generated run IDs and runtime Player instance identity.
  write_scope:
    - docs/plans/active-slices.yaml
    - docs/analysis/
    - shared/actionTypes.ts
    - shared/types.ts
    - server/socket.ts
    - server/Player/socket.ts
    - server/Player/socket.test.ts
    - server/lib/socketRateLimit.ts
    - server/lib/socketRateLimit.test.ts
    - src/store/modules/status.ts
    - src/store/modules/status.test.ts
    - src/routes/Player/modules/playerVisualizer.ts
    - src/routes/Player/modules/playerVisualizer.test.ts
    - src/routes/Player/components/Player/
    - src/routes/Player/components/PlayerController/
    - src/routes/Orchestrator/
  protected_paths:
    - server/Rooms/
    - server/Guest/
    - server/VideoProxy/
    - server/HydraPresets/
    - src/routes/Queue/
    - src/routes/Account/
    - package.json
    - package-lock.json
    - flake.nix
    - e2e/
    - .github/workflows/
  verification:
    - npm test -- server/Player/socket.test.ts server/lib/socketRateLimit.test.ts
    - npm test -- src/routes/Player src/routes/Orchestrator src/store/modules/status.test.ts
    - npm run slices:check
    - npm run typecheck
    - npm run lint
    - npm test
  acceptance:
    - Accepted Hydra broadcasts include server-generated visualizerRunId, visualizerCodeHash, and visualizerAcceptedAt; client-provided values are ignored.
    - PLAYER_EMIT_VISUALIZER_APPLIED is accepted only from the pinned Player identity {socketId, playerInstanceId}.
    - Unknown, stale, superseded, malformed, oversized, or wrong-player applied events do not overwrite room applied state.
    - Player emits applied state once per run ID only after Hydra eval and first tick succeed.
    - Orchestrator keeps Selected, Loaded in preview, Sent/Synced, and Applied on Player distinct.
    - Saved preset rows and gallery rows show Applied on Player only on exact applied metadata matches.
  non_goals:
    - Do not implement player media video source sync in this slice.
    - Do not change room policy, preset CRUD, queue architecture, Nix/package files, workflows, or gallery collaborator-send policy.
    - Do not resume or merge redux-modernization.
  notes:
    - "High-risk and security-sensitive: touches server player socket contracts, visualizer runtime, and Orchestrator applied-state display."
    - Player identity is runtime-only and generated by PlayerController on mount.
    - Phase 7B must wait for this slice to merge.
    - Overlaps blocked redux-modernization Player visualizer and Orchestrator paths; serialize ahead of redux-modernization.
    - Merged to main on 2026-05-21 as 1b380e58.
  next:
    - cleanup

- id: player-video-source-sync
  status: merged_to_main
  owner: codex
  branch: phase-7b-player-video-source-sync
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 110
  goal: Sync Hydra initVideo sources in Player and Orchestrator preview to the current Player MP4 media clock after applied-runtime truth lands.
  write_scope:
    - docs/plans/active-slices.yaml
    - docs/analysis/
    - server/Player/socket.ts
    - server/Player/socket.test.ts
    - shared/types.ts
    - src/store/modules/status.ts
    - src/routes/Player/components/Player/
    - src/routes/Player/components/PlayerController/
    - src/routes/Orchestrator/
    - src/lib/videoProxyOverride.ts
    - src/lib/videoProxyOverride.test.ts
  protected_paths:
    - server/Rooms/
    - server/Guest/
    - server/VideoProxy/
    - server/HydraPresets/
    - src/routes/Queue/
    - src/routes/Account/
    - package.json
    - package-lock.json
    - flake.nix
    - e2e/
    - .github/workflows/
  verification:
    - npm test -- server/Player/socket.test.ts src/lib/videoProxyOverride.test.ts src/routes/Player src/routes/Orchestrator src/store/modules/status.test.ts
    - npm run slices:check
    - npm run typecheck
    - npm run lint
    - npm test
  acceptance:
    - Player status exposes mediaId, mediaType, queueId, position, isPlaying, and statusAt for current media clock.
    - Server accepts sanitized source-binding summaries only from the pinned Player applied emitter.
    - Player identity registration is unthrottled or applied emits retry until the server can accept the pinned identity.
    - Orchestrator preview creates and cleans up a muted shadow video for current MP4 media only.
    - Shadow video seeks on media change, mirrors pause/play, and resyncs when drift exceeds 0.75s.
    - Active Player MP4 media intentionally overrides any Hydra sN.initVideo(...) URL while MP4 media clock is available.
    - Existing external/random initVideo behavior remains fallback when no current MP4 Player media exists.
  non_goals:
    - Do not start before visualizer-applied-runtime-truth merges.
    - Do not add exact cross-device frame sync.
    - Do not change room policy, preset CRUD, queue architecture, package, Nix, or workflow files.
  notes:
    - "This slice depends on Phase 7A accepted/applied runtime truth."
    - "The explicit source policy is product intent: Player MP4 wins over artist-authored initVideo URLs while current MP4 media is available."
    - "Red-team revision 2026-05-21: include source-binding protocol truth, borrowed media element cleanup rules, exact clock units, and Player identity race hardening."
    - Overlaps blocked redux-modernization Player visualizer and Orchestrator paths; serialize ahead of redux-modernization.
    - Merged to main on 2026-05-21 as bfa265c1.
  next:
    - cleanup

- id: deterministic-hydra-video-binding
  status: merged_to_main
  owner: codex
  branch: phase-7b1-deterministic-hydra-video-binding
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 115
  goal: Harden Hydra initVideo binding so Orchestrator waits for required Player MP4 shadow media instead of falling back to random external video.
  write_scope:
    - docs/plans/active-slices.yaml
    - docs/analysis/
    - src/lib/detectCameraUsage.ts
    - src/lib/detectCameraUsage.test.ts
    - src/lib/videoProxyOverride.ts
    - src/lib/videoProxyOverride.test.ts
    - src/routes/Orchestrator/components/HydraPreview.tsx
    - src/routes/Orchestrator/components/HydraPreview.test.tsx
    - src/routes/Player/components/Player/PlayerVisualizer/HydraVisualizer.tsx
    - src/routes/Player/components/Player/PlayerVisualizer/HydraVisualizer.applied.test.tsx
    - src/routes/Player/components/Player/PlayerVisualizer/HydraVisualizer.camera.test.tsx
    - e2e/fixtures/
  protected_paths:
    - server/
    - shared/actionTypes.ts
    - shared/types.ts
    - package.json
    - package-lock.json
    - flake.nix
    - .github/workflows/
    - e2e/*.spec.ts
  verification:
    - npm test -- src/lib/detectCameraUsage.test.ts src/lib/videoProxyOverride.test.ts src/routes/Orchestrator/components/HydraPreview.test.tsx src/routes/Player/components/Player/PlayerVisualizer/HydraVisualizer.applied.test.tsx src/routes/Player/components/Player/PlayerVisualizer/HydraVisualizer.camera.test.tsx
    - npm run slices:check
    - npm run typecheck
    - npm run lint
    - npm test
  acceptance:
    - HydraPreview waits for a Player MP4 shadow provider only when current Hydra code uses s0-s3.initVideo.
    - Audio-only, mouse-only, initImage, and initScreen patches render immediately under a current Player MP4 clock.
    - Required provider missing never creates fallback videos, calls Math.random, emits fallback binding, video-ready, or applied state.
    - Provider null-to-present and media identity changes re-evaluate once and bind all s0-s3 initVideo sources to the current borrowed provider.
    - Position/statusAt-only drift updates do not re-evaluate Hydra code or spam applied emits.
    - Shadow video cleanup removes only HydraPreview-owned shadow elements from the hidden Hydra video mount.
  non_goals:
    - Do not change server socket protocol, shared action constants, room policy, preset CRUD, package, Nix, workflows, or route guards.
    - Do not add exact frame sync.
    - Do not add media files for manual smoke targets.
  notes:
    - High-risk client runtime hardening touches HydraVisualizer and videoProxyOverride.
    - Red-team scope covers fallback suppression, borrowed video lifecycle, applied-state truth, and resource cleanup.
    - Cousteau and Close Encounters are manual local-library smoke targets.
    - Overlaps blocked redux-modernization Player visualizer and Orchestrator paths; serialize ahead of redux-modernization.
    - Merged to main on 2026-05-21 as c8d9f5f1.
  next:
    - cleanup

- id: orchestrator-preview-output-model
  status: merged_to_main
  owner: codex
  branch: docs/orchestrator-preview-output-model
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 120
  goal: Define the Orchestrator preview/output mental model before any Player Live mirroring work.
  write_scope:
    - docs/plans/active-slices.yaml
    - docs/ARCHITECTURE.md
    - docs/architecture/orchestrator-preview-output-model.md
    - docs/architecture/orchestrator-synthesis-ui-style-guide.md
    - docs/architecture/orchestrator-preset-operator-ux.md
    - docs/analysis/orchestrator_preview_output_model_2026_05_21.md
    - docs/analysis/current_state_2026_03_05.md
  protected_paths:
    - src/
    - server/
    - shared/
    - package.json
    - package-lock.json
    - flake.nix
    - e2e/
    - .github/workflows/
  verification:
    - rg -n "orchestrator-preview-output-model" docs/ARCHITECTURE.md docs/architecture
    - rg -n "Local Preview|Player Output|Player Live|Applied on Player" docs/architecture docs/analysis
    - npm run slices:check
    - npm run typecheck
  acceptance:
    - Durable docs define Local Preview, Preview using Player MP4, Preview waiting for Player media, Fallback external source, Applied on Player, Player Output, and Player Live.
    - The local Hydra preview is never described as Live, Player Output, Now Playing, or On Display.
    - Preset operator docs are reconciled with Phase 7A applied-state truth without rewriting historical analysis.
    - The architecture index links the new durable guide.
  non_goals:
    - Do not implement Player Live streaming, snapshots, or mirroring.
    - Do not change server/socket protocol, shared action types, runtime rendering, route guards, preset CRUD, package, Nix, workflow, or e2e files.
    - Do not rewrite historical dated analysis; supersede only the Applied-is-future-only point in a new dated note.
    - Do not resume or merge redux-modernization.
  notes:
    - "Lifecycle requirement 2026-05-21: register planned before creating the worktree, mark in_progress only after the worktree exists, and mark merged_to_main only after the slice lands."
    - Historical Apple HIG research is archival guidance for visible state, clear mental models, direct manipulation, immediate feedback, and avoiding hidden modes; it is not current Apple platform doctrine.
    - This slice is docs-only and sets product language for later Orchestrator UI work.
    - Merged to main on 2026-05-21 as 34c859c9.
  next:
    - cleanup

- id: orchestrator-presentation-truth-model
  status: merged_to_main
  owner: codex
  branch: phase-8-orchestrator-presentation-truth-model
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 130
  goal: Make Orchestrator Stage preview truth legible through a pure presentation model and Local Preview UI labels without Player Live mirroring.
  write_scope:
    - docs/plans/active-slices.yaml
    - docs/analysis/orchestrator_presentation_truth_model_2026_05_28.md
    - docs/analysis/current_state_2026_03_05.md
    - src/routes/Orchestrator/components/orchestratorPresentationModel.ts
    - src/routes/Orchestrator/components/orchestratorPresentationModel.test.ts
    - src/routes/Orchestrator/components/HydraPreview.tsx
    - src/routes/Orchestrator/components/HydraPreview.test.tsx
    - src/routes/Orchestrator/components/HydraPreview.css
    - src/routes/Orchestrator/components/orchestratorColorAudit.test.ts
  protected_paths:
    - server/
    - shared/actionTypes.ts
    - shared/types.ts
    - src/routes/Player/
    - package.json
    - package-lock.json
    - flake.nix
    - e2e/
    - .github/workflows/
  verification:
    - npm test -- src/routes/Orchestrator/components/orchestratorPresentationModel.test.ts src/routes/Orchestrator/components/HydraPreview.test.tsx src/routes/Orchestrator/components/orchestratorStatus.test.ts
    - npm test -- src/routes/Orchestrator/components/orchestratorColorAudit.test.ts
    - npm run slices:check
    - npm run typecheck
    - npm run lint
  acceptance:
    - HydraPreview overlay labels the current surface as Local Preview and never uses Live, Player Output, Now Playing, or On Display for preview status.
    - The pure presentation model derives preview, audio, and player-output states from explicit inputs.
    - Player MP4 plus initVideo shows waiting when the provider is absent and Preview using Player MP4 when the provider exists.
    - initVideo without current Player MP4 uses safe preset/external video-source copy without claiming successful binding.
    - Audio-only, mouse-only, initImage, and initScreen patches render immediately and create no shadow MP4 video.
    - Existing OrchestratorStatusStrip, PresetTree Applied-on-Player behavior, HydraVisualizer props, server/socket/shared contracts, and Player rendering remain unchanged.
  non_goals:
    - Do not implement Player Live streaming, snapshots, mirroring, WebRTC transport, or canvas capture.
    - Do not migrate PresetTree or rewrite OrchestratorStatusStrip.
    - Do not change server/socket protocol, shared action/type contracts, Player rendering, route guards, preset CRUD, package, Nix, workflow, or e2e files.
    - Do not claim external video successful binding from HydraPreview.
  notes:
    - Standard client UX/runtime slice; no high-risk Player visualizer files are in scope.
    - This starts the presentation truth architecture while wiring only the Stage/HydraPreview path.
    - Future Player Live can extend playerOutput states in a later high-risk architecture slice.
    - Merged to main on 2026-05-29 as cc373624.
  next:
    - cleanup

- id: orchestrator-status-truth-vocabulary
  status: merged_to_main
  owner: codex
  branch: phase-9-orchestrator-status-truth-vocabulary
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 140
  goal: Unify the Orchestrator presentation-truth vocabulary by promoting orchestratorPresentationModel.ts into the single source of label constants and broadcast wording, then sourcing the broadcast status label/tone and the Applied-on-Player badge from it, without touching server/socket/shared contracts, Player rendering, route guards, preset CRUD, or implementing Player Live mirroring.
  write_scope:
    - docs/plans/active-slices.yaml
    - docs/analysis/orchestrator_status_truth_vocabulary_2026_05_29.md
    - docs/analysis/current_state_2026_03_05.md
    - src/routes/Orchestrator/components/orchestratorPresentationModel.ts
    - src/routes/Orchestrator/components/orchestratorPresentationModel.test.ts
    - src/routes/Orchestrator/components/orchestratorStatus.ts
    - src/routes/Orchestrator/components/orchestratorStatus.test.ts
    - src/routes/Orchestrator/components/PresetTree.tsx
    - src/routes/Orchestrator/components/PresetTree.test.tsx
    - src/routes/Orchestrator/components/OrchestratorStatusStrip.test.tsx
  protected_paths:
    - server/
    - shared/actionTypes.ts
    - shared/types.ts
    - src/routes/Player/
    - src/routes/Orchestrator/views/
    - src/routes/Orchestrator/components/HydraPreview.tsx
    - src/routes/Orchestrator/components/HydraVisualizer.tsx
    - src/routes/Orchestrator/components/StagePanel.tsx
    - src/routes/Orchestrator/components/hydraPreviewUtils.ts
    - src/routes/Orchestrator/components/OrchestratorStatusStrip.tsx
    - package.json
    - package-lock.json
    - flake.nix
    - e2e/
    - .github/workflows/
  verification:
    - npm test -- src/routes/Orchestrator/components/orchestratorPresentationModel.test.ts src/routes/Orchestrator/components/orchestratorStatus.test.ts src/routes/Orchestrator/components/PresetTree.test.tsx src/routes/Orchestrator/components/OrchestratorStatusStrip.test.tsx
    - npm test -- src/routes/Orchestrator/components/orchestratorColorAudit.test.ts
    - npm run slices:check
    - npm run typecheck
    - npm run lint
  acceptance:
    - The presentation model exports canonical label constants (Local Preview, Applied on Player, Broadcast ready) and getOrchestratorPresentationModel output is unchanged.
    - The broadcast idle/ready status reads "Broadcast ready" with tone "neutral" (no longer "Preview ready"/tone "live"), sourced from the shared vocabulary.
    - The PresetTree Applied-on-Player badge text is sourced from the shared APPLIED_ON_PLAYER_LABEL constant with unchanged isApplied behavior.
    - The forbidden-term guard matches the exact capitalized token "Live" and exempts the legitimate connection/role truths "Camera live" and "Host live coding"; no broadcast or preset-applied label uses Live, Player Output, Now Playing, or On Display.
    - Camera relay status (Camera live, tone live), the camera pipeline Live wording, and the authority "Host live coding" label remain unchanged.
    - OrchestratorStatusStrip component, HydraPreview, HydraVisualizer props, server/socket/shared contracts, and Player rendering remain unchanged; the camera pill still maps to tone live.
  non_goals:
    - Do not implement Player Live streaming, snapshots, mirroring, WebRTC transport, or canvas capture.
    - Do not add new playerOutput truth states beyond noPlayer and playerPresentNotMirrored.
    - Do not rewrite OrchestratorStatusStrip or migrate PresetTree behavior; only swap label/tone sources.
    - Do not change camera-relay vocabulary (Camera live / pipeline Live) or the authority Host live coding label; they are real connection/role-state truths.
    - Do not promote the sibling PresetTree badge literals (Selected, Loaded in preview, Start, Cam) in this slice; only Applied on Player is promoted because the arch doc assigns that cross-surface term to preset rows.
    - Do not correct the stale CLAUDE.md/AGENTS.md vitest-alias guidance here; that is a separate trivial docs slice.
    - Do not change server/socket protocol, shared action/type contracts, Player rendering, route guards, preset CRUD, package, Nix, workflow, or e2e files.
  notes:
    - "User decisions locked 2026-05-29: broadcast idle label = 'Broadcast ready', tone = 'neutral'; vitest doc-truth correction split into a separate trivial slice."
    - Standard client UX slice; no high-risk Player visualizer, camera, room-access, or proxy files in scope.
    - Finishes the Phase 8 presentation-truth line by extending the model vocabulary to the two remaining Orchestrator status surfaces (orchestratorStatus.ts line 67 ships 'Preview ready'/tone 'live'; PresetTree.tsx line 310 ships a free 'Applied on Player' literal).
    - "Adversarial hardening locked 2026-05-29: forbidden-term match is case-sensitive on token 'Live' with an explicit allowlist for 'Camera live' and 'Host live coding'; RED tests assert against an exported label constant whose value lands at GREEN so the wording decision does not block RED."
    - The Player Live mirroring architecture spec (the runner-up proposal) is the documented follow-on high-risk docs-first slice and is intentionally deferred until after this coherence finish.
    - Merged to main on 2026-05-29 as b10a82d0.
  next:
    - cleanup

- id: orchestrator-presentation-truth-hardening
  status: merged_to_main
  owner: codex
  branch: phase-10-orchestrator-presentation-truth-hardening
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 150
  goal: Close the two LOW residue findings in the Local Preview clarity track before opening Player Live - the untested preview-truth to CSS-class mapping (the default 'local' truth maps to a .statusLocal selector that does not exist in HydraPreview.css, so the most common preview state renders silently unstyled) and usingPlayerMp4 firing on shadow-<video> existence rather than decoded frames - by extracting a pure, vitest-resolvable truth-to-class mapping helper, adding a neutral/muted .statusLocal selector, and gating usingPlayerMp4 on the same readyState>=2 && videoWidth>0 && videoHeight>0 condition HydraVisualizer already uses, without editing HydraVisualizer, server, socket, shared, or Player rendering.
  write_scope:
    - docs/plans/active-slices.yaml
    - docs/analysis/orchestrator_presentation_truth_hardening_2026_05_30.md
    - docs/analysis/current_state_2026_03_05.md
    - src/routes/Orchestrator/components/orchestratorPresentationModel.ts
    - src/routes/Orchestrator/components/orchestratorPresentationModel.test.ts
    - src/routes/Orchestrator/components/HydraPreview.tsx
    - src/routes/Orchestrator/components/HydraPreview.test.tsx
    - src/routes/Orchestrator/components/HydraPreview.css
    - src/routes/Orchestrator/components/orchestratorColorAudit.test.ts
  protected_paths:
    - server/
    - shared/actionTypes.ts
    - shared/types.ts
    - src/routes/Player/
    - src/routes/Orchestrator/components/orchestratorStatus.ts
    - src/routes/Orchestrator/components/orchestratorStatus.test.ts
    - src/routes/Orchestrator/components/PresetTree.tsx
    - src/routes/Orchestrator/components/PresetTree.test.tsx
    - src/routes/Orchestrator/components/OrchestratorStatusStrip.tsx
    - src/routes/Orchestrator/components/OrchestratorStatusStrip.test.tsx
    - src/routes/Orchestrator/components/StagePanel.tsx
    - src/routes/Orchestrator/components/hydraPreviewUtils.ts
    - src/routes/Orchestrator/views/
    - package.json
    - package-lock.json
    - flake.nix
    - e2e/
    - .github/workflows/
  verification:
    - npm test -- src/routes/Orchestrator/components/orchestratorPresentationModel.test.ts src/routes/Orchestrator/components/HydraPreview.test.tsx
    - npm test -- src/routes/Orchestrator/components/orchestratorColorAudit.test.ts
    - npm test -- src/routes/Orchestrator/components/orchestratorStatus.test.ts src/routes/Orchestrator/components/PresetTree.test.tsx src/routes/Orchestrator/components/OrchestratorStatusStrip.test.tsx
    - npm run slices:check
    - npm run typecheck
    - npm run lint
  acceptance:
    - The presentation model exports a pure, vitest-resolvable mapping from every OrchestratorPreviewTruth value (off, local, waitingForPlayerMedia, usingPlayerMp4, externalVideoSource) to a status CSS class key, and a truth-table test cross-checks each mapped .status<X> selector against HydraPreview.css read as raw text via fs.readFileSync (separate from the color-literal audit).
    - HydraPreview.css contains a neutral/muted .statusLocal selector for the default preview state using only Solarized var(--orch-*) tokens, so the most common preview state is no longer silently unstyled and orchestratorColorAudit.test.ts stays green.
    - HydraPreview.tsx derives the status class from the shared helper instead of an inline template literal.
    - usingPlayerMp4 (Preview using Player MP4) is shown only when the shadow Player media <video> has decoded frames (readyState >= 2 && videoWidth > 0 && videoHeight > 0), mirroring HydraVisualizer.isRemoteVideoRenderable; before decode the overlay shows Waiting for Player media. HydraVisualizer itself is not modified.
    - The readyState gate's interaction with the Hydra mount is analyzed and tested - hasPlayerMediaVideoElement also feeds shouldMountHydra via preview !== 'waitingForPlayerMedia', so a test must prove the shadow <video> decodes while Hydra is unmounted (no mount deadlock), and the ADR documents the mount-gate interaction.
    - getOrchestratorPresentationModel output is byte-equivalent for existing inputs (the readyState gate is applied in HydraPreview input derivation; no new model input is added - the model already accepts a hasPlayerMediaVideoElement boolean).
    - Phase 9 surfaces are unchanged - orchestratorStatus.ts (Broadcast ready / Camera live / Host live coding), PresetTree Applied-on-Player badge, OrchestratorStatusStrip component, server/socket/shared contracts, and Player rendering all remain unchanged; orchestratorStatus/PresetTree/OrchestratorStatusStrip tests stay green.
    - No preview status label uses Live, Player Output, Now Playing, or On Display; the FORBIDDEN_PREVIEW_TERMS guard and color audit stay green.
  non_goals:
    - Do not implement Player Live streaming, snapshots, mirroring, WebRTC transport, or canvas capture; that is the intended (unregistered) docs-first follow-on.
    - Do not edit HydraVisualizer.tsx; only mirror its readyState>=2 && videoWidth>0 && videoHeight>0 renderability condition inside HydraPreview input derivation.
    - Do not change orchestratorStatus.ts, PresetTree.tsx, or OrchestratorStatusStrip.tsx; this slice does not touch broadcast/preset/strip vocabulary.
    - Do not promote the deferred PresetTree sibling badge literals (Selected, Loaded in preview, Start, Cam); that remains out of scope.
    - Do not correct the stale CLAUDE.md/AGENTS.md vitest-alias guidance here; that is a separate trivial docs slice.
    - Do not add new playerOutput truth states; playerOutput stays noPlayer | playerPresentNotMirrored until the Player Live spec defines extensions.
    - Do not resume or merge redux-modernization.
    - Do not change server/socket protocol, shared action/type contracts, Player rendering, route guards, preset CRUD, package, Nix, workflow, or e2e files.
  notes:
    - "User decisions locked 2026-05-30: hardening-first sequencing (this slice before the Player Live spec); the default 'local' preview status pill is neutral/muted."
    - Standard client UX/runtime TDD slice; no high-risk Player visualizer, camera, room-access, or proxy files are in write scope. HydraVisualizer is READ-ONLY reference for the renderability condition only (real file src/routes/Player/components/Player/PlayerVisualizer/HydraVisualizer.tsx, covered by the src/routes/Player/ protected path).
    - "Findings verified on main HEAD 3e7d6758: getPreviewTruth defaults to 'local' (orchestratorPresentationModel.ts:78) producing class statusLocal at HydraPreview.tsx:294, but HydraPreview.css has no .statusLocal selector (rg statusLocal src/ empty). usingPlayerMp4 keys on Boolean(playerMediaVideoElement) (HydraPreview.tsx:235) set the instant the <video> is created (setVideo line 88), before any decode; HydraVisualizer's real gate is readyState>=2 && videoWidth>0 && videoHeight>0 (lines 41-43)."
    - "Adversarial HIGH-SEVERITY caveat locked: hasPlayerMediaVideoElement feeds shouldMountHydra (preview !== 'waitingForPlayerMedia') which gates the Hydra mount, so re-gating on decode delays the mount, not only the label - add a decode-while-unmounted test and re-rate this risk to medium/medium."
    - "CSS-module trap: under config/vitest.config.ts there is no CSS-modules transform, so importing styles yields undefined keys; the mapping test MUST read HydraPreview.css raw text via fs.readFileSync (orchestratorColorAudit.test.ts pattern), never the resolved import object. The color audit only scans color literals, not selector existence - keep the selector-presence test separate."
    - jsdom has no real video decoder; HydraPreview.test.tsx must stub readyState/videoWidth/videoHeight via Object.defineProperty on the shadow <video> to simulate the decoded transition (deliberate harness simulation).
    - Overlaps blocked redux-modernization Orchestrator paths (its write_scope src/routes/Orchestrator/components/ contains this slice's scope); serialize ahead of redux-modernization and do not run both branches in parallel.
    - Intended (unregistered) follow-on after this slice - player-live-mirroring-architecture-spec (docs-first), then player-live-mirroring-runtime (Security-Sensitive); control-plane cleanup + redux decommission is parallel non-blocking debt (slices:check exits 0 today).
    - Merged to main on 2026-05-30 as 91fe0736.
  next:
    - cleanup

- id: control-plane-merged-branch-cleanup
  status: planned
  owner: codex
  branch: chore/control-plane-cleanup
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 160
  goal: Clear the merged-branch/worktree cleanup backlog so slices:check emits zero WARNs before the Orchestrator UI push - remove the 11 stale worktrees, delete the 18 merged local branches and their origin counterparts, and prune the corresponding merged_to_main records from the control plane, leaving refactor/redux-modernization untouched.
  write_scope:
    - docs/plans/active-slices.yaml
    - docs/analysis/control_plane_cleanup_2026_05_30.md
  protected_paths:
    - src/
    - server/
    - shared/
    - package.json
    - package-lock.json
    - flake.nix
    - .github/workflows/
    - docs/architecture/
  verification:
    - npm run slices:check
    - npm run typecheck
  acceptance:
    - 'git worktree list shows only the main worktree; the 11 stale phase/design/docs worktrees are removed.'
    - 'All 18 merged local branches are deleted (git branch shows only main plus refactor/redux-modernization); origin counterparts are deleted and git fetch --prune is run.'
    - 'The 17 merged_to_main records (15 phase/docs/design plus rollback-2026-02-12-cleanup plus style-branch-cleanup) are pruned from active-slices.yaml, leaving only redux-modernization and the planned slices.'
    - 'npm run slices:check exits 0 with zero WARNs and zero errors.'
    - 'refactor/redux-modernization (local branch, origin branch, and YAML record) is untouched by this slice.'
  non_goals:
    - 'Do not delete, merge, or rescope refactor/redux-modernization; its decommission is the separate redux-modernization-decommission slice.'
    - Do not change any product code, server, shared, or architecture-doc content.
    - 'Do not use git branch -D force on any branch; rely on git branch -d merge-safety as the redux guardrail.'
  notes:
    - 'User decision locked 2026-05-30: clean now via record-pruning to zero WARNs, redux untouched.'
    - 'Execution order is load-bearing: (1) git worktree remove each of the 11 stale worktrees (all verified clean, no --force); (2) git worktree prune; (3) git branch -d the 18 merged branches; (4) git push origin --delete the 15 origin-resident merged branches; (5) git fetch --prune; (6) prune the 17 merged_to_main records from active-slices.yaml.'
    - 'WARN-to-zero requires record-pruning: slices:check branchExists is local-OR-origin, so deleting only local branches leaves WARNs for origin-resident branches.'
    - 'Recordless debt: chore-work-control-slices is merged plus local plus origin with no slice record - include it in the branch deletions.'
    - 'Recovery path: all 18 branches are ancestors of main so commits persist on origin/main; record pre-cleanup SHAs and rely on reflog before gc.'
    - 'Control-plane writes serialize ahead of the other planned docs slices (orchestrator-player-live-decision, redux-modernization-decommission): land this YAML edit first, then they rebase.'
  next:
    - implementation

- id: orchestrator-player-live-decision
  status: planned
  owner: codex
  branch: docs/orchestrator-player-live-decision
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 170
  goal: Decide, docs-first, whether the Orchestrator stays an honest local approximate preview (Option A), adds periodic snapshot/poster frames of actual Player output (Option B), or adds a real stream/mirror of Player output (Option C) - recording the decision in a durable ADR before any UX polish or runtime work.
  write_scope:
    - docs/plans/active-slices.yaml
    - docs/architecture/orchestrator-player-live-decision.md
    - docs/architecture/orchestrator-preview-output-model.md
    - docs/architecture/orchestrator-synthesis-ui-style-guide.md
    - docs/architecture/orchestrator-preset-operator-ux.md
    - docs/analysis/orchestrator_player_live_decision_2026_05_30.md
    - docs/analysis/current_state_2026_03_05.md
  protected_paths:
    - src/
    - server/
    - shared/
    - package.json
    - package-lock.json
    - flake.nix
    - e2e/
    - .github/workflows/
  verification:
    - rg -n "orchestrator-player-live-decision" docs/architecture docs/ARCHITECTURE.md
    - npm run slices:check
    - npm run typecheck
  acceptance:
    - 'The ADR (orchestrator-player-live-decision.md) presents Options A/B/C neutrally with evaluation criteria covering engineering cost, security/authz surface, latency/bandwidth, failure/degraded states, multi-Player ambiguity, and browser constraints.'
    - 'The ADR documents a recommended lean (B as pragmatic target, A as zero-cost fallback, C deferred behind named triggers) while leaving the final decision to user review.'
    - 'For each option the ADR records the OrchestratorPlayerOutputTruth extension it implies (today noPlayer or playerPresentNotMirrored) as a design artifact only, with no code change.'
    - 'All existing Player Live references (orchestrator-preview-output-model.md, orchestrator-synthesis-ui-style-guide.md, orchestrator-preset-operator-ux.md) are reconciled to point at the neutral decision ADR instead of pre-assuming mirroring.'
    - The architecture index links the new ADR.
  non_goals:
    - Do not implement Player Live streaming, snapshots, mirroring, WebRTC transport, or canvas capture.
    - 'Do not change runtime code: no src/, server/, shared/, package, Nix, e2e, or workflow edits.'
    - Do not edit orchestratorPresentationModel.ts or add playerOutput truth states; the truth-union extension is recorded as a design artifact only.
    - Do not finalize the Orchestrator UX status language; that is the downstream Gate 3a slice.
  notes:
    - 'User decision locked 2026-05-30: present A/B/C neutrally and decide at review, with a documented lean toward B.'
    - 'Supersedes the unregistered player-live-mirroring-architecture-spec note (Phase 9/10 records) which pre-assumed mirroring (Option C); this is a neutral decision ADR, not a mirroring spec.'
    - 'Blocks Gate 3a (orchestrator-ux-architecture-pass): the preview-vs-Player-output truth tier and Stage-strip layout cannot be finalized until this lands. Only about 20 percent of the status vocabulary is gated; the rest can be drafted in parallel.'
    - 'Option C, if ever chosen, becomes a future Security-Sensitive runtime slice (reverse-WebRTC of the Player canvas can re-broadcast a guest-sourced camera feed - full red-team plus High-Risk protocol).'
    - 'Control-plane writes serialize behind control-plane-merged-branch-cleanup: rebase on it before merge.'
  next:
    - implementation

- id: redux-modernization-decommission
  status: planned
  owner: codex
  branch: docs/redux-modernization-decommission
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 175
  goal: Formally retire the blocked refactor/redux-modernization branch (its headline Orchestrator API-lab feature already shipped on main as 087e3e83; it is 2 ahead and 182 behind main and not an ancestor) so the Gate 3 implementation slices can claim the Orchestrator write scope without a phantom serialization conflict.
  write_scope:
    - docs/plans/active-slices.yaml
    - docs/analysis/redux_modernization_decommission_2026_05_30.md
    - docs/analysis/current_state_2026_03_05.md
  protected_paths:
    - src/
    - server/
    - shared/
    - package.json
    - package-lock.json
    - flake.nix
    - .github/workflows/
    - docs/architecture/
  verification:
    - npm run slices:check
    - npm run typecheck
  acceptance:
    - 'The redux-modernization record status is moved from blocked to a terminal decommissioned state with a decision note (or removed) in active-slices.yaml.'
    - 'An ADR (redux_modernization_decommission_2026_05_30.md, Context/Decision/Consequences) records that the headline feature already shipped on main as 087e3e83, the branch is 2 ahead and 182 behind and not an ancestor, the conflict surface, and the decision to retire rather than reconcile.'
    - 'Any genuinely-unique salvageable delta is identified for separate re-implementation off main (do not cherry-pick the stale branch); if none, that is stated explicitly.'
    - The local and origin refactor/redux-modernization branches are deleted only after the decision is recorded.
    - The serialize-ahead-of-redux-modernization constraint on prior Orchestrator slices is no longer live once this lands.
  non_goals:
    - Do not reconcile or merge the stale branch into main.
    - 'Do not cherry-pick the 182 stale commits onto a long-lived branch; any salvage is a fresh minimal TDD change off main.'
    - Do not change product code in this slice; salvage is a separate slice.
  notes:
    - 'User decision locked 2026-05-30: register redux decommission as a gating slice before Gate 3 implementation.'
    - 'Gating prerequisite for Gate 3b..3f: redux-modernization write_scope (src/routes/Orchestrator/components/ plus views/ plus Player/PlayerVisualizer/) is the exact surface those impl slices edit, and slices:check does not enforce write_scope disjointness, so the collision is process-enforced only.'
    - 'Likely salvage candidate to evaluate: f0614609 send-ack-for-unchanged-code - verify whether main Phase 7A ack/run-id logic already covers it before salvaging.'
    - 'Control-plane writes serialize with the other planned slices on active-slices.yaml and current_state; rebase as needed.'
  next:
    - implementation

- id: rollback-2026-02-12-cleanup
  status: merged_to_main
  owner: unassigned
  branch: rollback/2026-02-12
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 900
  goal: Track cleanup for the already-merged rollback branch.
  write_scope: []
  protected_paths: []
  verification:
    - git merge-base --is-ancestor rollback/2026-02-12 main
  acceptance:
    - Local and remote rollback branches are deleted after maintainers confirm cleanup.
  non_goals:
    - Do not change application code.
    - Do not recreate the rollback branch.
  notes:
    - Branch is contained in main.
    - No worktree is required for cleanup.
  next:
    - cleanup

- id: style-branch-cleanup
  status: merged_to_main
  owner: unassigned
  branch: style
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 901
  goal: Track cleanup for the already-merged style branch.
  write_scope: []
  protected_paths: []
  verification:
    - git merge-base --is-ancestor style main
  acceptance:
    - Local and remote style branches are deleted after maintainers confirm cleanup.
  non_goals:
    - Do not change application code.
    - Do not recreate the style branch.
  notes:
    - Branch is contained in main.
    - No worktree is required for cleanup.
  next:
    - cleanup
