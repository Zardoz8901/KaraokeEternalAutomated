# Gate 3 + HiG-bug closeout: remaining slices by change-type (decided 2026-05-30).
# Deferred (NOT in this closeout): HiG #14 folder-action discoverability, #15 PresetPicker menu rebuild,
# #16 keyboard resize separator. See docs/analysis/orchestrator_hig_ux_backlog_2026_05_30.md.

- id: orchestrator-preset-row-stability
  status: in_progress
  owner: codex
  branch: phase-17-orchestrator-preset-row-stability
  base: main
  merge_order: 230
  worktree:
    path: /home/steph/projects/KaraokeEternalAutomated
    state: present
  goal: 'Apply the PresetTree D1/D3.2 row-stability mitigations from the visual-system direction: reserve a fixed-height no-wrap badge lane, order badges strongest-truth-first (Applied, Loaded, Selected, Start, Cam, Gallery) in both visual and accessible-name order, decouple row Select from explicit Load, and render Gallery identity on the row - no label/status-ownership/server/socket/Player changes.'
  write_scope:
    - docs/plans/active-slices.yaml
    - docs/analysis/orchestrator_preset_row_stability_2026_05_31.md
    - docs/analysis/current_state_2026_03_05.md
    - src/routes/Orchestrator/components/PresetTree.tsx
    - src/routes/Orchestrator/components/PresetTree.css
    - src/routes/Orchestrator/components/PresetTree.test.tsx
    - src/routes/Orchestrator/components/PresetBrowser.tsx
    - src/routes/Orchestrator/components/PresetBrowser.test.tsx
  protected_paths:
    - server/
    - shared/actionTypes.ts
    - shared/types.ts
    - src/routes/Player/
    - src/routes/Orchestrator/components/orchestratorStatus.ts
    - src/routes/Orchestrator/components/orchestratorPresentationModel.ts
    - src/routes/Orchestrator/components/HydraPreview.tsx
    - package.json
    - package-lock.json
    - flake.nix
    - e2e/
    - .github/workflows/
  verification:
    - npm test -- src/routes/Orchestrator/components/PresetTree.test.tsx src/routes/Orchestrator/components/PresetBrowser.test.tsx
    - npm test -- src/routes/Orchestrator/components/orchestratorColorAudit.test.ts
    - npm run slices:check
    - npm run typecheck
    - npm run lint
  acceptance:
    - 'Row click/Enter selects only; explicit Load button performs local preview load and sets Loaded in preview.'
    - 'Preset row badges render and announce strongest-truth-first: Applied on Player, Loaded in preview, Selected, Start, Cam, Gallery.'
    - 'Gallery identity renders on gallery preset rows, not only the gallery folder header.'
    - 'Badge lane is fixed-height/no-wrap with overflow/ellipsis so badge changes do not reflow row geometry mid-set.'
    - 'No label/status-ownership/server/socket/Player changes; send/manage gates remain unchanged.'
  non_goals:
    - Do not implement row-local send acknowledgements or mobile Presets-tab dots; those are a later D2/D6 slice.
    - Do not change camera pipeline labels, status strip semantics, Player Output snapshot, or broader no-raw-hue audit.
    - Do not change server/shared/Player/package/Nix/workflow/e2e.
  notes:
    - 'Consumes the --orch-applied role added by orchestrator-color-role-map.'
    - 'CSS layout is source-asserted; local Nix Chromium e2e remains blocked, so no screenshot gate.'
  next:
    - implementation
