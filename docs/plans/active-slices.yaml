# Gate 3 + HiG-bug closeout: remaining slices by change-type (decided 2026-05-30).
# Deferred (NOT in this closeout): HiG #14 folder-action discoverability, #15 PresetPicker menu rebuild,
# #16 keyboard resize separator. See docs/analysis/orchestrator_hig_ux_backlog_2026_05_30.md.

- id: orchestrator-role-routing-migrate
  status: in_progress
  owner: codex
  branch: phase-13-orch-role-routing-migrate
  base: main
  merge_order: 260
  worktree: { path: /home/steph/projects/KaraokeEternalAutomated, state: present }
  goal: 'Route Orchestrator action, feedback, error, and reference CSS off raw Solarized hue tokens onto existing semantic role tokens and add a file-wide raw-hue sweep with the deferred PresetBrowser error handoff.'
  write_scope:
    - docs/plans/active-slices.yaml
    - src/routes/Orchestrator/components/PresetTree.css
    - src/routes/Orchestrator/components/CodeEditor.css
    - src/routes/Orchestrator/components/PresetPicker.css
    - src/routes/Orchestrator/components/PresetBrowser.css
    - src/routes/Orchestrator/components/ApiReference.css
    - src/routes/Orchestrator/components/orchestratorColorAudit.test.ts
  protected_paths:
    - server/
    - shared/
    - src/routes/Player/
    - src/routes/Orchestrator/views/OrchestratorView.css
    - src/routes/Orchestrator/components/StagePanel.css
    - src/routes/Orchestrator/components/PresetBrowser.tsx
    - package.json
    - package-lock.json
    - flake.nix
    - e2e/
    - .github/workflows/
  verification:
    - npm test -- src/routes/Orchestrator/components/orchestratorColorAudit.test.ts
    - npm test
    - npm run typecheck
    - npm run lint
    - npm run slices:check
  acceptance:
    - 'The new file-wide raw-hue sweep over PresetTree, CodeEditor, PresetPicker, PresetBrowser, and ApiReference passes with only the documented neutral-chrome plus deferred .error allowlist stripped.'
    - 'Existing D8.5 ceiling test remains warn-form: toBeLessThanOrEqual(28), with collectRawHueRefsInStateClasses unchanged.'
    - 'Action, feedback, and reference classes in scope route through existing role tokens and tone recipe; no token definitions, TSX, runtime, or StagePanel changes.'
  non_goals:
    - Do not add or modify token definitions.
    - Do not flip the existing D8.5 ceiling assertion to equality or lower its number.
    - Do not route PresetBrowser.css .error; it remains the deferred raw red handoff.
    - Do not touch StagePanel.css, OrchestratorView.css, TSX/runtime files, server, shared, Player, camera, socket, Redux, routes, package, Nix, workflow, or e2e.
  notes:
    - 'cssBlock first-match blind spots are expected for grouped-leading-member selectors; direct source containment is required for those assertions.'
    - 'PresetBrowser .error remains raw var(--orch-red) until the empty-error-states slice can add non-text affordance and contrast-safe markup.'
  next:
    - implementation
