- id: redux-modernization
  status: blocked
  owner: unassigned
  branch: refactor/redux-modernization
  base: main
  worktree:
    path: /home/steph/projects/KaraokeEternalAutomated-redux-modernization
    state: missing
  merge_order: 10
  goal: Modernize Orchestrator API/editor/reference flows while preserving Hydra visualizer behavior.
  write_scope:
    - src/routes/Orchestrator/components/
    - src/routes/Orchestrator/views/
    - src/routes/Player/components/Player/PlayerVisualizer/
  protected_paths:
    - server/
    - src/routes/Queue/
    - src/routes/Account/
    - docs/plans/
    - .github/workflows/
  verification:
    - npm test -- src/routes/Orchestrator/components/ApiReference.test.tsx src/routes/Orchestrator/components/CodeEditor.test.tsx src/routes/Player/components/Player/PlayerVisualizer/hydraEvalCode.test.ts
    - npm run typecheck
    - npm run lint
  acceptance:
    - Existing Orchestrator code editor and Hydra visualizer behavior remains covered by focused tests.
    - Branch has a dedicated worktree before review or implementation resumes.
    - Branch is rebased or reconciled with main before merge because it is behind main.
  non_goals:
    - Do not change queue, room, auth, guest, or video proxy behavior.
    - Do not alter server socket action contracts.
    - Do not modify repo work-control files from this feature branch.
  notes:
    - Branch exists locally and on origin but no dedicated worktree is currently checked out.
    - Branch is not contained in main.
    - Touches high-risk Player visualizer files; follow AGENTS.md high-risk protocol before code changes.
  next:
    - implementation
    - review

- id: hydra-control-boundary
  status: merged_to_main
  owner: codex
  branch: phase-2-hydra-control-boundary
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 20
  goal: Enforce the Hydra room control boundary so collaborators can only broadcast server-validated saved presets while owners/admins retain live-code control.
  write_scope:
    - docs/plans/active-slices.yaml
    - docs/analysis/
    - server/Player/
    - server/socket.ts
    - server/serverWorker.ts
    - server/Rooms/router.ts
  protected_paths:
    - src/routes/Orchestrator/
    - src/routes/Player/components/Player/PlayerVisualizer/
    - server/VideoProxy/
    - server/lib/Database.ts
    - server/lib/bcrypt.ts
    - flake.nix
  verification:
    - npm test -- server/Player/socket.test.ts
    - npm run slices:check
    - npm run typecheck
    - npm run lint
    - npm test
  acceptance:
    - Owner/admin sockets can still broadcast arbitrary Hydra code.
    - Collaborator and guest sockets cannot broadcast arbitrary Hydra code.
    - Collaborator and guest sockets can broadcast only server-loaded DB presets allowed by room policy.
    - Visualizer replay state is attributed and cleared on room lifecycle cleanup.
  non_goals:
    - Do not change Orchestrator UI behavior in this slice.
    - Do not alter Hydra browser execution isolation.
    - Do not change video proxy, auth crypto, database wrapper, queue, or Nix behavior.
  notes:
    - "Authority decision locked 2026-05-19: preset-limited collaborators, arbitrary code only for owner/admin."
    - Gallery presets are client-bundled and remain owner/admin-only until a server-validatable gallery source exists.
    - High-risk visualizer runtime policy change; follow AGENTS.md high-risk and red-team protocols.
    - Merged to main on 2026-05-19.
  next:
    - cleanup

- id: orchestrator-synthesis-style-guide
  status: merged_to_main
  owner: codex
  branch: docs/orchestrator-synthesis-style-guide
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 30
  goal: Document Orchestrator-first synthesis UI style guidance grounded in NN/g heuristics, progressive disclosure, complex-app UX, and accessible synthesis controls.
  write_scope:
    - docs/plans/active-slices.yaml
    - docs/ARCHITECTURE.md
    - docs/architecture/
    - docs/analysis/current_state_2026_03_05.md
  protected_paths:
    - src/
    - server/
    - shared/
    - package.json
    - package-lock.json
    - flake.nix
    - .github/workflows/
  verification:
    - rg -n "orchestrator-synthesis-ui-style-guide|Orchestrator Synthesis UI Style Guide" docs
    - npm run slices:check
    - npm run typecheck
    - npm run lint
  acceptance:
    - Style guide is discoverable from architecture docs.
    - Guide defines Orchestrator-first progressive synthesis UI principles.
    - Guide records owner/admin live-code vs collaborator preset-only UX authority.
    - Slice record tracks completion without force-adding ignored analysis tracker files.
  non_goals:
    - Do not change runtime UI behavior in this slice.
    - Do not alter server socket contracts or room-policy enforcement.
    - Do not redesign the whole app-wide visual system yet.
  notes:
    - "User decision locked 2026-05-19: Orchestrator-first repo style guide using progressive synth UI."
    - Docs-only slice; no production-code TDD target.
    - Merged to main on 2026-05-19.
  next:
    - cleanup

- id: orchestrator-capability-ux
  status: merged_to_main
  owner: codex
  branch: phase-2-orchestrator-capability-ux
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 40
  goal: Align Orchestrator UI dispatch paths with the backend Hydra authority boundary so collaborators use preset-operator workflows and cannot trigger raw-code sends from UI paths.
  write_scope:
    - docs/analysis/
    - docs/plans/active-slices.yaml
    - src/routes/Orchestrator/
  protected_paths:
    - server/
    - shared/actionTypes.ts
    - shared/types.ts
    - src/routes/Player/
    - src/components/App/
    - package.json
    - package-lock.json
    - flake.nix
    - .github/workflows/
  verification:
    - npm test -- src/routes/Orchestrator/components/orchestratorCapabilities.test.ts src/routes/Orchestrator/components/PresetTree.test.tsx src/routes/Orchestrator/components/PresetPicker.test.tsx src/routes/Orchestrator/views/orchestratorViewHelpers.test.ts
    - npm run slices:check
    - npm run typecheck
    - npm run lint
  acceptance:
    - Raw code send dispatch is gated in Orchestrator handlers, not only hidden in UI.
    - Collaborators and guests can send only saved DB presets when both room policies allow it.
    - Gallery/raw-code send paths are manager-only.
    - PresetTree click and keyboard send paths share the same send predicate.
    - Existing preset/folder CRUD permissions remain admin-or-author.
  non_goals:
    - Do not change backend socket contracts or enforcement.
    - Do not change room prefs APIs or admin room-policy management.
    - Do not perform the Solarized restyle in this slice.
    - Do not resume or merge redux-modernization.
  notes:
    - "Red-team fix locked 2026-05-19: gate dispatch functions and keyboard/click send paths, not only rendering."
    - Overlaps blocked redux-modernization Orchestrator paths; serialize this slice and do not run both branches in parallel.
    - "Implemented 2026-05-20: capability helper gates Orchestrator raw-code, gallery, and preset send UX without backend contract changes."
    - Merged to main on 2026-05-20.
  next:
    - cleanup

- id: orchestrator-solarized-foundation-ux
  status: merged_to_main
  owner: codex
  branch: design/orchestrator-solarized-foundation-ux
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 50
  goal: Apply a Solarized, token-driven Orchestrator foundation with Figma-level shell polish, status clarity, and deterministic visual QA.
  write_scope:
    - docs/analysis/
    - docs/architecture/orchestrator-synthesis-ui-style-guide.md
    - docs/plans/active-slices.yaml
    - src/routes/Orchestrator/
  protected_paths:
    - server/
    - shared/actionTypes.ts
    - shared/types.ts
    - src/routes/Player/
    - src/routes/Queue/
    - src/routes/Account/
    - package.json
    - package-lock.json
    - flake.nix
    - .github/workflows/
  verification:
    - npm test -- src/routes/Orchestrator/views/orchestratorLayout.test.ts src/routes/Orchestrator/views/orchestratorViewHelpers.test.ts src/routes/Orchestrator/components/orchestratorCapabilities.test.ts
    - npm test -- src/routes/Orchestrator/components/orchestratorStatus.test.ts src/routes/Orchestrator/components/OrchestratorStatusStrip.test.tsx src/routes/Orchestrator/components/orchestratorColorAudit.test.ts src/routes/Orchestrator/components/PresetBrowser.test.ts
    - npm run slices:check
    - npm run typecheck
    - npm run lint
    - npm run build
  acceptance:
    - Orchestrator shell, Stage header, CodeEditor, CodeMirror theme, Hydra syntax highlighting, presets, API reference, and scoped Orchestrator modals use Solarized tokens instead of one-off GitHub/neon colors.
    - Stage header status strip shows Host live coding, Preset operator, or Policy blocked, plus broadcast and camera state without colliding with preset, camera, or buffer controls.
    - Every PresetBrowser Modal receives the scoped Orchestrator modal class.
    - Deterministic color audit passes for touched Orchestrator files.
    - Desktop and mobile visual QA is completed through authenticated Playwright when feasible, otherwise documented as manual QA with exact viewport evidence.
  non_goals:
    - Do not change backend socket contracts or enforcement.
    - Do not change room prefs APIs or admin room-policy management.
    - Do not redesign app-wide queue, account, player, or shared Modal defaults.
    - Do not add new runtime dependencies.
    - Do not resume or merge redux-modernization.
  notes:
    - "User decision locked 2026-05-20: Orchestrator-first foundation + shell polish, code plus docs, include CodeMirror, Stage-header status strip, scoped Orchestrator modals."
    - CodeMirror tokenization includes both CodeEditor.tsx theme colors and hydraHighlightStyle.ts syntax colors.
    - "Implementation note 2026-05-20: authenticated Playwright screenshots remain manual QA until Nix browser wiring is fixed; the reusable setup/login auth path exists in e2e/smoke.spec.ts."
    - "Validation note 2026-05-20: local Chromium e2e is blocked on Nix because the dev shell does not provide a Nix-built browser or PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH; downloaded Playwright Chromium fails with stub-ld."
    - Merged to main on 2026-05-20 as 8657488f.
    - Stale merged worktrees and branches exist and are cleanup candidates, but cleanup is outside this slice.
  next:
    - cleanup

- id: orchestrator-menu-productization
  status: merged_to_main
  owner: codex
  branch: phase-3-orchestrator-menu-productization
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 60
  goal: Make Visuals a discoverable first-class app area with role-aware Orchestrator host/operator workspaces and Nix-runnable Playwright visual QA.
  write_scope:
    - docs/plans/active-slices.yaml
    - docs/analysis/
    - docs/architecture/orchestrator-synthesis-ui-style-guide.md
    - flake.nix
    - src/components/Navigation/
    - src/components/App/Routes/
    - src/store/middlewareOptions.ts
    - src/store/store.ts
    - src/store/store.test.ts
    - src/routes/Orchestrator/
    - e2e/
  protected_paths:
    - server/
    - shared/actionTypes.ts
    - shared/types.ts
    - src/routes/Player/
    - src/routes/Queue/
    - src/routes/Account/
    - package.json
    - package-lock.json
    - .github/workflows/
  verification:
    - npm test -- src/components/Navigation src/components/App/Routes src/routes/Orchestrator
    - npm run slices:check
    - npm run typecheck
    - npm run lint
    - npm run build
    - nix develop -c npm run test:e2e -- --project=chromium
  acceptance:
    - Visuals appears in bottom app navigation with visible icon-and-label treatment when /orchestrator route access allows it.
    - Bottom app navigation labels remain visible and unclipped with four items on mobile.
    - Orchestrator host layout preserves Presets/API, Stage, and Code; operator/browse layout expands Stage and hides Code/API.
    - Orchestrator status distinguishes Host live coding, Preset operator, Browse only, and Policy blocked.
    - Preset panel empty and policy states explain saved preset availability and send authority.
    - Nix dev shell runs Chromium Playwright without the downloaded-browser stub-ld failure.
  non_goals:
    - Do not change backend socket contracts or enforcement.
    - Do not change room prefs APIs or admin room-policy management.
    - Do not modify package scripts, package lockfiles, or GitHub workflows.
    - Do not alter Player, Queue, Account, server, or shared action/type contracts.
    - Do not resume or merge redux-modernization.
  notes:
    - "User decision locked 2026-05-20: Visuals is a first-class bottom-nav destination serving host plus operator audiences."
    - This slice overlaps blocked redux-modernization Orchestrator component/view paths; serialize this slice ahead of redux-modernization and do not run both branches in parallel.
    - Library escape is navigation, not an Orchestrator workspace tab.
    - Merged to main on 2026-05-20 as 5cdbafae.
  next:
    - cleanup

- id: orchestrator-workspace-model
  status: planned
  owner: codex
  branch: phase-4-orchestrator-workspace-model
  base: main
  worktree:
    path: /home/steph/projects/KaraokeEternalAutomated-phase-4-orchestrator-workspace-model
    state: missing
  merge_order: 70
  goal: Extract Orchestrator runtime decisions into tested pure workspace models plus a thin integration hook while preserving visualizer, camera, and preset behavior.
  write_scope:
    - docs/plans/active-slices.yaml
    - docs/analysis/
    - src/routes/Orchestrator/views/
    - src/routes/Orchestrator/components/
    - e2e/
  protected_paths:
    - server/
    - shared/actionTypes.ts
    - shared/types.ts
    - src/routes/Player/
    - src/routes/Queue/
    - src/routes/Account/
    - package.json
    - package-lock.json
    - flake.nix
    - .github/workflows/
  verification:
    - npm test -- src/routes/Orchestrator
    - npm test
    - npm run typecheck
    - npm run lint
    - npm run slices:check
    - npm run build
    - nix develop -c npm run test:e2e -- --project=chromium
  acceptance:
    - OrchestratorView delegates runtime decisions to a workspace hook/model while still composing StagePanel, CodeEditor, PresetBrowser, ApiReference, and OrchestratorStatusStrip directly.
    - useOrchestratorWorkspace remains a thin integration facade over Redux, reducer injection, current-room fetch, camera sender wiring, timers, and pure helper state machines.
    - Focused tests preserve send ack timing, remote replacement/counting, pre-edit sync, preset-index auto-apply, resize bounds, and panel normalization behavior.
    - Camera relay lifecycle remains unchanged and manual camera/orchestrator validation is logged in the analysis note.
    - No backend, socket, shared contract, package, Nix, or workflow changes are introduced.
  non_goals:
    - Do not change backend socket contracts or enforcement.
    - Do not change room prefs APIs, route guards, or admin room-policy management.
    - Do not introduce a player-applied visualizer ack protocol.
    - Do not add new user-visible preset indicators in this slice.
    - Do not resume or merge redux-modernization.
  notes:
    - "User decision locked 2026-05-21: next architecture slice is Orchestrator workspace-model extraction after Visuals productization."
    - This slice overlaps blocked redux-modernization Orchestrator component/view paths; serialize this slice ahead of redux-modernization and do not run both branches in parallel.
    - High-risk because useCameraSender ownership moves behind the workspace facade; keep the camera hook unchanged and record manual validation outcomes.
    - The hook must return typed plain prop objects and state, not JSX.
  next:
    - implementation
    - review

- id: rollback-2026-02-12-cleanup
  status: merged_to_main
  owner: unassigned
  branch: rollback/2026-02-12
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 900
  goal: Track cleanup for the already-merged rollback branch.
  write_scope: []
  protected_paths: []
  verification:
    - git merge-base --is-ancestor rollback/2026-02-12 main
  acceptance:
    - Local and remote rollback branches are deleted after maintainers confirm cleanup.
  non_goals:
    - Do not change application code.
    - Do not recreate the rollback branch.
  notes:
    - Branch is contained in main.
    - No worktree is required for cleanup.
  next:
    - cleanup

- id: style-branch-cleanup
  status: merged_to_main
  owner: unassigned
  branch: style
  base: main
  worktree:
    path: null
    state: not_required
  merge_order: 901
  goal: Track cleanup for the already-merged style branch.
  write_scope: []
  protected_paths: []
  verification:
    - git merge-base --is-ancestor style main
  acceptance:
    - Local and remote style branches are deleted after maintainers confirm cleanup.
  non_goals:
    - Do not change application code.
    - Do not recreate the style branch.
  notes:
    - Branch is contained in main.
    - No worktree is required for cleanup.
  next:
    - cleanup
