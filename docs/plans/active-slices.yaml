# Gate 3 + HiG-bug closeout: remaining slices by change-type (decided 2026-05-30).
# Deferred (NOT in this closeout): HiG #14 folder-action discoverability, #15 PresetPicker menu rebuild,
# #16 keyboard resize separator. See docs/analysis/orchestrator_hig_ux_backlog_2026_05_30.md.

- id: orchestrator-preset-state-legend
  status: in_progress
  owner: claude
  branch: phase-17-orch-legend-drop-gallery
  base: main
  worktree: { path: /home/steph/projects/KaraokeEternalAutomated, state: present }
  merge_order: 270
  goal: 'Render the persistent preset-state legend (PRESET_STATE_LEGEND) unconditionally in the Presets panel header, and drop the Gallery/QR badge entirely while keeping the word Gallery in the preset row accessible name. No camera relay / player / server / SQL change.'
  write_scope:
    - docs/plans/active-slices.yaml
    - docs/analysis/orchestrator_preset_state_legend_2026_06_03.md
    - src/routes/Orchestrator/components/orchestratorPresentationModel.ts
    - src/routes/Orchestrator/components/orchestratorPresentationModel.test.ts
    - src/routes/Orchestrator/components/PresetTree.tsx
    - src/routes/Orchestrator/components/PresetTree.css
    - src/routes/Orchestrator/components/PresetTree.test.tsx
    - src/routes/Orchestrator/components/PresetBrowser.tsx
    - src/routes/Orchestrator/components/PresetBrowser.test.tsx
  protected_paths:
    - server/
    - shared/
    - src/lib/webrtc/
    - src/routes/Player/
    - src/routes/Orchestrator/components/StagePanel.tsx
    - src/routes/Orchestrator/components/orchestratorStatus.ts
    - src/components/Icon/icons.ts
    - docs/architecture/orchestrator-visual-language.md
    - package.json
    - package-lock.json
  verification:
    - npm test -- src/routes/Orchestrator/components/PresetTree.test.tsx src/routes/Orchestrator/components/PresetBrowser.test.tsx src/routes/Orchestrator/components/orchestratorPresentationModel.test.ts src/routes/Orchestrator/components/orchestratorColorAudit.test.ts
    - npm test
    - npm run typecheck
    - npm run lint
    - npm run slices:check
  acceptance:
    - 'The Presets panel header renders a labeled legend (role=list, aria-label="Preset state key") with one item per PRESET_STATE_LEGEND entry, reusing the phase-16 glyph-circle vocabulary; it shows for viewers/guests with no management toolbar.'
    - 'The Gallery state badge no longer renders on preset rows and Gallery is absent from PRESET_STATE_GLYPHS and PRESET_STATE_LEGEND; the preset row aria-label still includes "Gallery" via the decoupled accessibleBadgeLabels path.'
    - 'The legend is non-interactive (no tabindex, no data-tree-focusable) and its meaning text uses --orch-text, never --orch-muted (S1 contrast lock).'
  non_goals:
    - Do not change camera relay runtime, WebRTC, server/socket/redux, Player routes, or status-strip vocabulary.
    - Do not edit the spec doc orchestrator-visual-language.md here — that is the separate docs slice (C).
    - Do not remove the QR_CODE icon registration from icons.ts (may have other consumers); only stop referencing it in the preset-state glyph map.
  notes:
    - 'Direct follow-up to phase-16 glyph-circle badges. Standard TDD slice; visual sign-off by Steph per visual-authority-division.'
  next:
    - implementation
